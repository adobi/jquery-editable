(function($) {
    
    $.fn.editable = function(options) {
        
        options = $.extend({}, $.fn.editable.defaults, options);
    };
    
    $.fn.editable.defaults = {
        
    }
    
    
}) (jQuery);